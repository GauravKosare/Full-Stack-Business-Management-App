NODE_ENV=development
PORT=3000

# Postgres connection string (Supabase project connection string in dev)
DATABASE_URL="postgresql://user:password@host:5432/postgres"

# Auth
JWT_SECRET="replace-with-a-long-random-string"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_CALLBACK_URL="http://localhost:3000/api/v1/auth/google/callback"
MOBILE_AUTH_REDIRECT_URL="myapp://auth"
WEB_APP_URL="http://localhost:3001"

# Brevo (transactional invite emails — optional, invites still work without it,
# just without an email being sent)
BREVO_API_KEY=""
BREVO_SENDER_EMAIL="gpkfree@gmail.com"
BREVO_SENDER_NAME="Business Management App"

# Realtime chat (Supabase Broadcast — server pushes new-message events to open browser
# tabs; without these, chat still works via the polling refresh, just without instant
# delivery). SUPABASE_SERVICE_ROLE_KEY is a secret — Project Settings > API > service_role.
SUPABASE_URL="https://onkycwbfwrddmnbcoqun.supabase.co"
SUPABASE_SERVICE_ROLE_KEY=""

# Razorpay (test mode keys)
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
RAZORPAY_WEBHOOK_SECRET=""
RAZORPAY_PLAN_PRO=""
RAZORPAY_PLAN_ENTERPRISE=""

# Misc
APP_URL="http://localhost:3000"
LOG_LEVEL="info"
